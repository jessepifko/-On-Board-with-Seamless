@model List<StartupContainer>

<a href ="\Launchpad\AddStartup">Add a Startup</a>

<a href ="\Launchpad\ViewDashboard">View Dashboard</a>


@foreach  (StartupContainer startup in Model)
{
    @startup.Fields.CompanyName
}